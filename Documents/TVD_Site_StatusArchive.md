# TecVooDoo Web Infrastructure -- Project Status ARCHIVE

Historical overflow from [TVD_Site_Status.md](TVD_Site_Status.md), split out 2026-07-18 during the v1.14 Living-Doc Hygiene sweep (the live doc had grown to ~60 KB vs its ~15 KB byte budget). This file holds older session history, retired Version-History rows, and completed Cloudflare-hardening detail. Read-on-demand only -- the live doc carries current state; open items were kept there, not moved here.

---

## Recent Session History (archived: 2026-06-26 and earlier)

### 2026-06-26 -- Brand reskin Phase 1+2 (shipped + live)
- **First real visual-identity overhaul, shipped + live-verified** (commit `1cd447d`, pushed `main`). Danny delivered the full **TecVooDoo Brand Package** (extracted to `TecVooDooMarketing\TecVooDoo Brand Package\`, disk-only/not git): brand guide PDF, RGB+CMYK palette, 3 typefaces, Hex mascot (dark/light), TEC/VOODOO logotype + logo-with-mascot, TvD lettermarks, circuit/grid patterns. Deleted the superseded `[DRAFT]` guide; the finalized guide is the single source. Original Drive zip kept at `E:\TecVooDoo\` as archive.
- **Did Phase 1 (color+identity) + Phase 2 (type) deliberately WITHOUT Enigma/Buster** -- both are commercial fonts whose webfont/self-hosting rights were unconfirmed at the time (Space Grotesk is OFL/safe). **Later ruled (2026-07-10): Enigma + Buster are graphics-only -- no webfont tier**, so the Space Grotesk fallback for `--font-display`/`--font-accent` is permanent (finalizing the swap-point comment is queued under Branding batch #3).
- **Changes (all in `site/`):** (1) real brand palette into `style.css` `:root` (Shadow `#221f1f`, Pin `#fff`, Current `#23ffd6`, Blood `#ec1c24`) + derived tokens, replacing off-brand `#0d0d0d`/`#00f0c0`/`#ff3366` and scattered hardcoded `rgba()`. (2) self-hosted Space Grotesk variable ttf + `OFL.txt` under `site/fonts/`; `@font-face` + body/display headings. (3) TvD lettermark nav across all 8 pages, logo-with-Hex hero on index, SVG favicon (was absent). (4) 1200x630 OG share image + full OG/Twitter meta on `index.html` (both were absent).
- **Env tooling note (reusable):** this box has no real Python (`python`/`python3` are Windows Store stubs), no ImageMagick (`convert` = Windows NTFS tool), no SVG rasterizer -- used **PowerShell `System.Drawing`** to composite the OG PNG, and an SVG favicon to avoid rasterizing.
- **Carryover (now in Branding batch #3):** Phase 3 patterns/Hex placements; interior-page OG images; `.ico`/PNG favicon fallback (needs a rasterizer this box lacks); Space Grotesk ships as `.ttf` not `.woff2`; unreferenced `tecvoodoo-logo-small.png` left in `site/images/`. Code+asset session; no canonical check performed.

### 2026-06-22 -- Brand-guide review + brand-voice copy alignment
- **Triggered by:** Rune sharing Danny Schmidt's first draft brand guide (`TecVooDooMarketing\[DRAFT] TVD Brand Guide.pdf`, dated 2026-06-15) — strong visual skeleton (camera-lens-eye voodoo doll mascot, cyan circuitry-as-stitching, pin motif carried into the TvD lettermarks; Enigma/Buster/Space Grotesk type; Shadow/Pin/Current/Blood palette) with placeholder copy + open creative questions.
- **Mascot named "Hex"** — chosen for the voodoo↔tech double meaning (hex = curse *and* hex code). Picked over Patch / Cursor / Bug.
- **8 keywords locked:** Mischievous · Macabre · Cursed · Handmade · Irreverent · Aberrant · Stitched · Alive. Iteration trail: started Playful/Gritty/Bold/Glitchy among them; Rune swapped Playful→Mischievous, Gritty→Stitched; then Bold→Cursed (Bold is generic/tests nothing; Cursed is ownable and threads mascot+writeup+palette), and Glitchy→Aberrant (keeps an anomaly/system-deviation edge so the keyword set doesn't lose its only tech-leaning word; "$10 clinical word among handmade ones" mirrors the tec-meets-voodoo thesis).
- **Copy written:** brand writeup (2 paras), 8 keywords, and the Hex mascot blurb → **`TecVooDooMarketing\TVD_BrandGuide_Copy.md`** (NEW). The marketing folder is **not under version control** (disk-only/Drive-backed, same as DAB), so this is the on-disk source of truth for the brand-guide *text*; hand to Danny to replace lorem-ipsum.
- **Site code change (committed):** brand voice line propagated to production. `site/index.html` hero subtitle and `site/about.html` "What is TecVooDoo?" opener both moved from "embrace the odd, the strange, and the dark" (a stacked-synonym line — same flaw we'd just rejected for the keyword list) to **"a little cute, a little cursed, and entirely their own"**, matching the brand writeup. about.html kept its "create games, write books, and build apps" verb variety; only the tail swapped. The "What We Do" subtitle ("Games that challenge, stories that disturb, experiences that linger") was left untouched — already the strongest copy on the page. Footer auto-bumped by the pre-commit hook.
- **Flagged to Danny (not site work):** (a) the "CMYK" palette gives hex codes, not real C/M/Y/K breakdowns — needs proper values (+ ideally Pantone for Blood/Current) before print use; (b) confirm commercial licensing for Enigma + Buster typefaces (Space Grotesk is OFL/open and fine).
- **No canonical version-check** this session (copy-only work, not a cold start); UniversalWorkflow marker unchanged at v1.13.

### 2026-06-09 -- itch.io channel logging + canonical maintenance
- **No site/code/infra changes.** Logging-only session.
- **itch.io studio page live** at `tecvoodoo.itch.io` (announced by Rune at session start) -- resolves the long-standing "Handle TBD" gap in [[reference-tvd-infrastructure]]. Studio bio + a **Blood Miner free playtest build** in-browser; Rune clarified it's a *data-collection playtest to tune the first few levels*, **not the full game and not a demo funnel** -- so the $3.99 premium Google Play positioning is untouched. Recorded in the marketing handle tracker (`TecVooDooMarketing\TVD_MarketingStrategy.md`, disk-only/Drive-backed -- not git) + the infra memory.
- **Site→itch integration deferred to a dedicated session** (Rune's call): port the **two other site games to itch -- DLYH and DAB** (BM already on itch as the playtest). DLYH is a Unity WebGL build (iframe-wrapped at `site/games/dlyh/index.html`, source `E:\Unity\DLYH\`) -- itch hosts WebGL natively, straightforward. DAB (`site/games/dots-and-boxes/index.html`) is HTML/JS with a **backend dependency** (Supabase auth/multiplayer/realtime + the Resend email Worker for invites) -- the port must confirm `api.tecvoodoo.com` + the Worker accept **CORS from the itch origin** (`itch.zone`/`*.hwcdn.net`) or DAB's multiplayer + invite break. After the ports, the site gets itch links + "Play in browser" paths on those cards.
- **Canonical check:** UniversalWorkflow.md v1.12 -> v1.13 (Tier-2 read-on-demand canonicals now in the session-start version-check). No web effect -- the web project holds no Tier-2 `Aligned to:` pointers. Marker bumped to v1.13; Web_Standards/Web_Workflow still v1.0, aligned.

### 2026-06-08 -- Games page content refresh
- **First site-code change since 2026-05-31.** Refreshed `site/games.html` game cards with real gameplay/key-art screenshots pulled from each title's `E:\Unity\<Project>\Documents\Marketing\` folder.
- **Roster changes:** removed **Shrunken Head Toss** (SH); added **Blood Miner** (BM, first commercial release -- "Coming Soon - Google Play", portrait key art, copy from `BM_SellSheet.md`) into the now-vacated "Coming Soon" slot; added **GRIMMORPG** to "Up Next" (point-and-click horror adventure, copy from its GDD); renamed **Hooked on Charon -> Hooked on Kharon** (proper brand spelling per Rune) with new shot; refreshed **DLYH** + **FearSteez** screenshots.
- **Images:** +4 (`blood-miner-keyart`, `grimmorpg-gameplay`, `hok-gameplay`, `fearsteez-gameplay`), 1 overwritten (`dlyh-gameplay`), -3 orphans (`sht-concept`, `fearsteez-concept`, `hooked-charon-concept`). All site image refs verified non-dangling; games.html ASCII-clean; footer auto-bumped by pre-commit hook.
- **Same-day follow-ups:** (a) DLYH graphic wasn't updating live -- overwritten in place under the same filename, so the unchanged URL served the stale browser/CDN cache; renamed to `dlyh-gameplay-v2.png` (commit `e837bf9`) to cache-bust. The other 4 updated fine (new filenames). (b) Three Up Next titles moved "Planning" -> "In Development" badges + matching subtitle, per Rune.
- **Carryover:** header/meta copy ("Word games with guillotines") now undersells the catalog; `dlyh-concept.png` 5.6 MB orphan still present. None blocking.
- **Also:** session-start canonical check bumped UniversalWorkflow marker v1.9 -> v1.12 (no web rule change; details in Canonical check note above).

### 2026-06-05 -- Canonical alignment maintenance
- No site/code/infra changes. Session-start check caught UniversalWorkflow.md v1.5 -> v1.9; assessed deltas (v1.6 one-step-at-a-time APPLIES, v1.7 git repo-health check APPLIES, v1.9 new-chat-trigger APPLIES, v1.8 Unity build/profiling N/A); bumped marker to v1.9. Ran v1.7 repo-health check: 1 pack / 8.66 MiB, healthy. Discovered DAB is not under version control (known gap, edit saved to disk only). Doc bumped to v7.

### 2026-05-31 -- Leak follow-ups + Cloudflare hardening
- **Prevention layer (the Resend follow-up):** added a dependency-free secret scanner to the site repo's `.githooks/pre-commit` -- blocks staged files by secret-file name pattern and greps staged blob content for credential token shapes (`re_`, `sk_`, `sk-`, `AKIA`, `xi-api-key`, PEM private-key headers); runs `gitleaks protect --staged` as a second layer only if gitleaks is on PATH (it is NOT installed on this machine). The hook excludes itself from the content scan since it carries the detection signatures. `.gitignore` gained `.env`/`*.key`/`*.pem`/`*API*.txt`/`credentials*.json`/`*secret*.txt`. Verified by decoy: a `re_` token in content and a `*API*.txt` filename were both blocked. Commit `1815892`, pushed.
- **ElevenLabs leak (cross-repo):** the GitGuardian-flagged key lived inline in `E:\Unity\M3AnimatedSeries` `.mcp.json` + `.claude/mcp.json` -- tracked in HEAD, committed in `774a88e`, and pushed to `origin/master`. Root cause: M3's `.gitignore` deliberately whitelisted `!/.mcp.json` + `!/.claude/` (opposite of this repo, which ignores them). Rune rotated + revoked the key (leaked copy now inert). Per the cross-project rule (this session may edit but not push another repo), edited M3's `.gitignore` to drop the two files and left it UNSTAGED; the remaining `rm --cached` + commit + history scrub + force-push is queued for an M3 session (see Active TODO).
- **Cloudflare:** identified the never-recorded "deferred CF work" as 4 zone security insights (zone `75d5d0f937fa670879de6906f35277c8`, Pro plan, confirmed via read-only graphql MCP). Implemented the one that's a site file -- `/.well-known/security.txt` (RFC 9116, points to the public contact page, Expires 2027-05-31), commit `7f830bb`, pushed. The other three were done in the dashboard during the session (paired walkthrough): WAF Cloudflare Managed Ruleset deployed (OWASP left off), Super Bot Fight Mode set to Block definitely-automated -- safe because `api.tecvoodoo.com` was confirmed DNS-only/grey-cloud, so Supabase API + WebSocket traffic bypasses the proxy -- and AI Labyrinth enabled-then-reverted (conflicts with the documented "LLM indexing is desirable" strategy in `TVD_AssetInventory.md`; "Block AI bots" + robots.txt AI directive flagged for the same review). All 4 insights addressed. Mid-session realized the "deferred CF work" was recorded all along in `TVD_AssetInventory.md` "Pending Cloudflare hardening" + memory -- it lists more open items (Brotli, SSL/TLS Full-strict verify, HTTP/3, Always-HTTPS, Workspace DKIM, disable Email Routing); updated that tracker with today's completions. CF DNS surfaced a missing DMARC record -- deployed `_dmarc` TXT in monitor mode (`p=none`, rua to runeduvall@); confirmed mail is Google Workspace (MX) + Resend (DKIM at `resend._domainkey`, `send.` via Amazon SES), correcting an initial wrong assumption of M365. Stale M365/Lync/GoDaddy DNS records noted for later cleanup. Confirmed CF MCP access is read-only + has no WAF/Bot tooling, so the toggles were necessarily dashboard work.
- **No production HTML changed.** Three site-repo commits this session: `1815892` (secret scan), `7f830bb` (security.txt), and this Status update.

### 2026-05-30 (PM) -- Resend API key leak remediation
- **Triggered by:** a GitGuardian email ("[TecVooDoo/tecvoodoo] Resend API Key exposed on GitHub", pushed 2026-05-30 13:00:54 UTC). The AM reorg's doc migration had committed `Documents/Archives/ResendAPI.txt` -- a one-line plaintext file holding the live Resend key `re_STPmUe7R...` -- in commit `816306b`.
- **Diagnosis:** the key was tracked in exactly one commit; no other credential files or secrets in the repo. The contact form (`contact.html`) is `mailto:`-based and never used Resend. Beta-reader signups (the original Resend justification) are fully retired -- grep finds no `beta_readers` UI anywhere in `site/`. The **only** live consumer of the Worker is the Dots and Boxes "Email a friend" invite ([games/dots-and-boxes/index.html:2764](../site/games/dots-and-boxes/index.html#L2764)).
- **Remediation:** (1) created a new Resend key + revoked the old `re_STPmUe7R...`; (2) rotated the Cloudflare Worker secret `RESEND_API_KEY` and redeployed (`tecvoodoo-email`, script version `e1315631`); (3) verified via Workers observability -- a real DAB invite produced `POST / -> 200`; (4) purged `ResendAPI.txt` from all history with `git filter-branch`, force-pushed `main` (`50a5eb9` -> `64f151c`), pruned local orphans; (5) confirmed `origin/main` carries zero references to the file or key string.
- **Caveat:** GitHub may still serve the old commit `816306b` by direct SHA from cache until its own GC runs; the key is revoked so that copy is inert. Full eviction would need a GitHub Support GC request -- not worth it.
- **Permissions note:** the force-push was soft-blocked by the auto-mode classifier (history rewrite is destructive); cleared by explicit user authorization. An agent cannot edit its own `.claude/settings.local.json` to grant force-push -- that is a hard self-modification block, so the allowlist edit is always a user action.
- **Outstanding (on Active TODO):** add `gitleaks` + secret-file ignores to the pre-commit hook; resolve the GitGuardian alert; rotate + scrub the older ElevenLabs key in `M3AnimatedSeries`.

### 2026-05-30 (AM) -- TVD studio reorg + doc-system buildout
- **Triggered by:** a cold-start agent failure earlier the same day — Rune opened a fresh chat to continue Cloudflare work; the agent couldn't recover where the prior session left off from memory alone. Diagnosed as a Status doc + studio structure problem, not an agent problem.
- **NO site code changes.** This was entirely a docs + filesystem reorg session.
- **Studio reorg:** flattened `E:\TecVooDoo\` from a nested `Projects\Documents\`, `Projects\Games\`, `Projects\Writing\`, `Projects\Other\TecVooDooWebsite\` structure into top-level `Studio\`, `Games\`, `Writing\`, `Retired\`, with the site repo `TecVooDooSite\` as a co-equal subfolder. `E:\Personal\` was carved out for non-TVD personal content. HumiliNation snapshot deleted entirely (423 MB recovered; GitHub mirror is authoritative). Synthosapians + The Nexus Party consolidated under `Retired\Synthosapians_NexusParty\`.
- **Site repo gained:** `CLAUDE.md` at root (agent orientation), `Documents\TVD_Site_Status.md` (renamed from `TecVooDoo_Web_Status.md` at the old `Projects\Other\TecVooDooWebsite\Documents\` location), `Documents\Archives\` with 49 flattened archived design docs (27 TV_* + 22 Supabase_*). Two commits on `main`: `816306b` Migrate web docs into repo, `cebfc09` Add CLAUDE.md for agent orientation. Pushed to `origin/main`.
- **Canonical layer:** relocated from `Projects\Documents\Canonical\` to `Studio\Canonical\` (was a Phase 4 atomic move with backup zip). Canonical references in this Status doc, `DAB_Status.md`, `TVD_MarketingStrategy.md`, and `Studio\TVD_AI_Rules.md` all rewritten in the same batch. Added 4 Tier 2 docs to `Studio\Canonical\Web\`: `PerProject_DocSystem.md` (spec), `PerProject_StatusTemplate.md` (skeleton), `PerProject_Adoption_Prompt.md` (retrofit walkthrough), `PerProject_MCP_Brief.md` (per-project MCP quick-ref).
- **Studio orientation:** new `Studio\INDEX.md` for cold-start agents landing studio-wide. `TecVooDoo_Projects.csv` bulk-updated to reflect post-reorg paths.
- **Permissions:** `E:\TecVooDoo\.claude\settings.local.json` expanded from 6 to 90 allowlist entries (WebSearch, dev-domain WebFetch, read-only MCP tools across Supabase / Cloudflare / Discord, git push/pull/gh CLI). Standing authorization to push `main` without per-push confirmation. Reload VSCode workspace for the settings to take effect.
- **Memory:** 4 new memory entries (`reference_unity_workspaces`, `reference_windows_reserved_name_files`, `feedback_push_to_main_authorized`, `feedback_doc_system_purpose`); 8 existing entries updated with new paths.
- **Google Drive coverage:** now sync covers the entire `E:\TecVooDoo\` root (was only `Projects\` pre-reorg).
- **What did NOT happen:** the Cloudflare work that originally triggered the fresh chat. Still on Active TODO. Reorg plan archived at `E:\TecVooDoo\Studio\Archives\TVD_StudioReorgPlan.md` for forensic reference.

### 2026-05-22 -- Post-crash recovery + canonical doc cleanup
- C: drive crashed earlier in the day; Claude memory and global git config were wiped. Project files on E: were intact.
- Cloudflare had an active API/Dashboard 503 incident at the same time -- separate from the local crash, but compounded the troubleshooting: MCP OAuth flows for the 7 Cloudflare servers all failed silently because OAuth callbacks couldn't land.
- Restored: `git config --global --add safe.directory E:/TecVooDoo/TecVooDooSite` (fixes "dubious ownership" on E: drive after C: reset).
- Killed 7 zombie `mcp-remote` node.exe processes from a pre-crash VS Code session that were holding OAuth callback ports; deleted stale `_lock.json` entries in `C:\Users\steph\.mcp-auth\`; verified all MCP servers (Supabase + all 7 Cloudflare) reconnect successfully.
- Refreshed footers on 9 production HTML files (8 in Status doc's list + `privacy.html` which had been missed). Stale dates from Dec 27, 2025 to Jan 16, 2026 + `(c) 2025` -> `May 22, 2026` + `(c) 2026`. Will be automated going forward via a pre-commit hook in the repo.
- Built out `E:\TecVooDoo\Projects\Documents\Canonical\Web\` with `INDEX.md`, `TecVooDoo_Web_Standards.md`, and `TecVooDoo_Web_Workflow.md`. Extracted code patterns, deploy pipeline, ops gotchas from this Status doc + DAB_Status.md + the legacy `TecVooDoo_AI_Rules.md`. Corrected the `sbClient` recommendation to `supabaseClient` to match deployed production code; principle (avoid `window.supabase` shadow) unchanged.
- This doc slimmed to project state only; rules/patterns/lessons all moved to canonical. Bumped to v3.

### 2026-02-12 -- Repo hygiene
- Removed `tmpclaude/` junk directory from the repo (`fc435a4`).
- Added `.gitignore` to keep similar accidental artifacts out of future commits.
- Removed the standalone Murder, Malady and Monsters chapter preview page and beta reader page (`5cdd10b`) -- the beta reader signup remained on the book detail pages.

### 2026-01-25 -- DLYH iframe top offset
- Fixed iframe top offset 60px -> 72px in `games/dlyh/index.html` to give the Unity build more vertical space and reduce UI overlap on mobile (`14b2a16`).

### 2026-01-16 -- DLYH iframe wrapper page
- Added `games/dlyh/index.html` as an iframe wrapper around the DLYH Unity build (`7c11128`). Provides unified TecVooDoo auth UI around the Unity game while the Unity project itself runs in the embedded frame.

### 2026-01-05 -- Status doc consolidation (v1)
- Consolidated 4 prior documents (`TV_Architecture` v8, `TV_DesignDecisions` v8, `TV_ProjectInstructions` v8, `TV_SiteDesign` v8) plus the Supabase set (`Supabase_*` v6) into this single Status doc. Archives preserved under `Archives/`.

---

## Cloudflare zone security -- completed insights (moved from Active TODO 2026-07-18)

Zone tecvoodoo.com `75d5d0f937fa670879de6906f35277c8` (Pro plan). The 4 security insights recovered 2026-05-31 were all closed 2026-05-31; full narrative in the 2026-05-31 session entry above. Remaining OPEN hardening items (Brotli, Always-HTTPS verify, HTTP/3, SSL Full-strict, Workspace DKIM, disable CF Email Routing, prune stale DNS cruft) stayed in the live doc's Active TODO.

- [x] security.txt configured -- `/.well-known/security.txt` added (RFC 9116, commit `7f830bb`)
- [x] Critical: WAF Managed Rules deployed -- Cloudflare Managed Ruleset active (action=Default, status=Default). OWASP Core Ruleset intentionally left OFF (too aggressive for the Supabase API traffic). Path: Security -> Security rules -> Managed rules -> "Go to web application exploits settings".
- [x] Moderate: Super Bot Fight Mode -- enabled with **Definitely automated traffic = Block**, Verified bots = Allow (it was toggled on but set to Allow/no-op until this change). Safe because `api.tecvoodoo.com` is **DNS-only / grey-cloud** (confirmed in DNS -> Records), so Supabase API + WebSocket traffic bypasses the CF proxy entirely. Path: Security -> Settings -> filter "Bot traffic".
- [x] Low: AI Labyrinth -- briefly enabled, then **reverted to OFF** 2026-05-31. It conflicts with the documented strategy (`TVD_AssetInventory.md` lists it under "intentionally skipped" because LLM indexing is *desirable* for indie discovery). The CF insight may re-flag it; leaving it off is intentional. FLAGGED: "Block AI bots" + the robots.txt AI directive are currently ON and contradict the same pro-discovery stance -- review.
- [x] **DMARC deployed in monitor mode 2026-05-31** -- added `_dmarc` TXT `v=DMARC1; p=none; rua=mailto:runeduvall@tecvoodoo.com; fo=1`. Mail is **Google Workspace** (MX `aspmx.l.google.com` + `alt1-4`), plus **Resend** for DAB invites (authenticated: DKIM at `resend._domainkey`, `send.` MX via Amazon SES). Upgrade path: after ~2-4 weeks of clean aggregate reports, tighten `p=none` -> `p=quarantine` -> `p=reject`.

---

## Version History (archived rows v1-v8)

| Version | Date | Summary |
|---------|------|---------|
| 8 | 2026-06-08 | Games page content refresh (`site/games.html`): removed Shrunken Head Toss, added Blood Miner (Coming Soon) + GRIMMORPG (Up Next), renamed Hooked on Charon -> Hooked on Kharon, refreshed DLYH/FearSteez/HOK screenshots with real gameplay art. +4/-3 images. Canonical marker v1.9 -> v1.12 (no web rule change). |
| 7 | 2026-06-05 | Canonical alignment maintenance only -- UniversalWorkflow marker v1.5 -> v1.9 (v1.6/v1.7/v1.9 apply, v1.8 N/A); repo-health check healthy; DAB-not-versioned gap logged. No site/code changes. |
| 6 | 2026-05-31 | Leak follow-ups + Cloudflare hardening: secret-scanning pre-commit hook + gitignore rules (`1815892`); ElevenLabs key rotated/revoked + M3 `.gitignore` fix staged for an M3 session; recovered + closed all 4 Cloudflare zone security insights (security.txt `7f830bb` + WAF/Bot/AI Labyrinth dashboard toggles), deployed DMARC in monitor mode, logged stale-DNS cleanup. No production HTML changed. |
| 5 | 2026-05-30 | Resend API key leak remediation: revoked + rotated the key, purged `ResendAPI.txt` from git history (force-push `64f151c`), verified `origin/main` clean. Prevention TODOs added (gitleaks pre-commit, ElevenLabs/M3AnimatedSeries follow-up). No site code changes. |
| 4 | 2026-05-30 | TVD studio reorg + doc-system buildout. Doc moved into repo (renamed from TecVooDoo_Web_Status.md), canonical paths rewritten, CLAUDE.md + Studio\INDEX.md + 4 Canonical Tier 2 PerProject_* docs added. No site code changes. |
| 3 | 2026-05-22 | Post-crash session: rules/patterns/lessons extracted to new `Canonical/Web/` layer; this doc slimmed to state-only. Footers updated across 9 production HTML files. Recent Session History expanded to cover the gap since v2. |
| 2 | 2026-01-25 | Fixed DLYH iframe height offset (top: 60px -> 72px) |
| 1 | 2026-01-05 | Initial consolidated document (replaces TV v8 + Supabase v6) |

---

**End of Status Archive**
